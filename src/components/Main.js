import { Route, Switch } from "react-router-dom";
import Index from "../pages/Index";
import Show from "../pages/Show";

const Main = (props) => {
  return (
    <main>
      <Switch></Switch>
    </main>
  );
};

export default Main;
